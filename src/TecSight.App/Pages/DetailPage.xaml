<UserControl x:Class="TecSight.App.Pages.DetailPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:TecSight.App.Controls"
             Foreground="{DynamicResource TextBrush}" Background="{DynamicResource BgBrush}">
    <DockPanel>
        <CheckBox x:Name="NoiseFilterBox" DockPanel.Dock="Top" Margin="16,12,16,0"
                  Content="{Binding Loc[Detail.HideNetworkNoise]}" Foreground="{DynamicResource TextBrush}"
                  Checked="NoiseFilter_Changed" Unchecked="NoiseFilter_Changed"
                  Visibility="Collapsed"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="16">
        <ItemsControl x:Name="Sections">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <GroupBox Header="{Binding Title}" MinWidth="340" MaxWidth="560" Margin="0,8,8,8" Padding="10" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1">
                        <ItemsControl ItemsSource="{Binding Rows}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="190"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="150"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Label}" Foreground="{DynamicResource SubTextBrush}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Value}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                        <controls:Sparkline Grid.Column="2" Values="{Binding Spark}" Height="24" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </GroupBox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
