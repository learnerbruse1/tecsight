# TecSight

一个查看 Windows 设备硬件清单与运行指标的桌面工具。目标是跨硬件兼容：在尽可能多的 Windows 电脑上都能展示完整信息，缺失的传感器优雅降级，不因某台机器读不到数据而崩溃。

## Language

**硬件清单（Hardware Inventory）**:
设备上静态存在的硬件及其静态属性（型号、容量、频率、固件版本等），随时间基本不变。
_Avoid_: 设备列表、配置信息、规格

**运行指标（Live Metrics）**:
随使用实时变化的数值（占用率、实时频率、温度、吞吐、电量等），按刷新周期更新。
_Avoid_: 状态、性能数据、运行状态

**传感器读数（Sensor Reading）**:
由主板/芯片/磁盘/显卡等暴露的单个可测量数值（温度、风扇转速、电压），带有单位与来源。
_Avoid_: 测量值、读数记录

**健康度（Health）**:
设备自诊断给出的健康结论（如磁盘 SMART 的良好/注意/危险）。
_Avoid_: 状态等级、评分

**快照（Snapshot）**:
某一时刻硬件清单与全部运行指标的完整集合，可导出为报告。
_Avoid_: 报告、导出文件

**刷新周期（Refresh Interval）**:
运行指标重新采集的时间间隔，默认 1 秒。
_Avoid_: 采样率、轮询间隔

**降级（Graceful Degradation）**:
当某类硬件或传感器无法读取（无权限、无驱动、硬件不支持）时，该处显示"不可用"并继续运行，而不是报错或崩溃。这是本项目的兼容性核心要求。
_Avoid_: 容错、兜底

**本地化（Localization）**:
界面文案支持中文与英文两种语言，可在运行时切换，默认跟随系统语言。硬件厂商名称等专有名词不翻译。
_Avoid_: 国际化、多语言、i18n

**外设（Peripheral）**:
接入设备的可枚举外围硬件（存储/键盘/鼠标/摄像头/音频/蓝牙/打印机/USB 等），按类型分类识别；内置硬件（如内置 NVMe）不属于外设。
_Avoid_: 设备、周边、附件