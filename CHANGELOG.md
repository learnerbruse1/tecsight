# Changelog

本项目所有显著变更均记录于此。

## [2.1.0] - 2026-08-26

### 新增
- 概览页新增「显存使用率」卡片，并按新布局重排：网络移到第二行第三列、电池移到原主板位置、删除主板卡片，其余卡片位置不变
- CPU 温度兜底：LibreHardwareMonitor 读不到 CPU 温度时，回退到 Windows 自带 ACPI 热区（`\\.\ThermalZone` IOCTL + WMI `MSAcpi_ThermalZoneTemperature`，均免管理员）
- 外设信息回填：修正失效的 `Win32_PnPEntity` 查询，并新增 SetupAPI（Nefarius.Utilities.DeviceManagement，MIT）、显示器 EDID 与 HidSharp 序列号/厂商回填

### 修复
- 修复中英文切换后左侧导航丢失选中状态，以及设置保存后点击界面可能出现的异常（导航项改为原地更新标题，不再重建列表）
- 修复内存使用率与「已用/总计」口径不一致（百分比改为按物理内存已用/总计计算，与显示数值一致）
- 修复导出 TXT/HTML/兼容性报告中的隐式截断（传感器/SMART/其他设备列表全部完整导出）
- 未处理异常弹窗改为每个会话只弹一次（每次异常仍写入日志），避免同一错误反复打断用户
- 进程 CPU 历史及时清理已退出 PID；历史快照读取零复制；折线渲染不再多次复制整段序列

### 兼容性
- LibreHardwareMonitor 打开失败后每 30 秒自动重试（不再永久放弃）
- 语言切换、主题切换、设置保存流程增加自动化回归测试（当前 231 项单元测试）

## [2.0.0] - 2026-08-23

### 变更
- 版本号升级为 2.0.0
- 同步更新安装程序、发布说明与中英文文档

## [1.2.0] - 2026-08-23

### 修复
- 修复大内存平台上 `MaxCapacity` 读取溢出导致的容量 N/A
- `Win32_PhysicalMemoryArray.MaxCapacity` 返回 `uint.MaxValue` 哨兵值时按未知处理，避免误报 4 TB
- 将 WMI 返回的 `CurrentClockSpeed=0` 视为未知，避免显示误导性的 `0 MHz`
- 将内存条 `Capacity`/`Speed`/`ConfiguredClockSpeed` 返回字符串 `"0"` 视为未知，避免显示 `0 B`/`0 MHz`
- 详情页解析内存容量时同样将 `0` 视为未知，兼容旧版缓存中的 `"0"` 值
- 将 WMI 返回的缓存、分辨率、刷新率、显卡显存、磁盘容量、生产年份等 `0` 值统一视为未知，避免显示无意义的 `0`
- WMI 布尔字段在属性缺失/null 时返回“未知”，不再误显示为 `否`
- WMI 数值字段在属性缺失/null 时返回“未知”，不再被 `Convert` 转成无意义的 0
- 内存拓扑插槽数为 `0` 时显示不可用，而不是 `0 插槽`
- 外设 USB 磁盘无型号时回退为 `USB Disk`，可移动磁盘无盘符时回退为 `Removable Disk`
- EDID 显示器信息仅在有有效名称或厂商时才采用，否则回退到 `Win32_DesktopMonitor`，避免空显示行
- EDID 显示器名称解码保留非 ASCII 字符，中文显示器名称不再被过滤成空
- 网络吞吐采集时排除虚拟网卡实例，降低 TAP/VPN/蓝牙 PAN 等造成的数值虚高
- 网络适配器过滤同时接受 `PhysicalAdapter=null` 的真实接口，避免个别 WMI 返回空值时漏掉网卡
- 概览页在所有网卡均为虚拟时不再把虚拟网卡当成主网卡，仅在无真实网卡时兜底
- 外设页空读卡器不再显示为 `0 B`；补充 `USBHub` 分类
- 外设检测增加 `Win32_Keyboard` / `Win32_PointingDevice` 回退，修复鼠标等 HID 外设偶发检测不到
- 外设页合并硬件清单中的显示器、音频、USB、键盘、鼠标、打印机，确保所有外设都显示
- 键盘/鼠标/音频清单补充 PNPDeviceID 与厂商，外设页按 PNP ID 去重，修复同一物理设备重复显示
- 硬件清单中的物理网卡并入外设页 network 分类，外设页覆盖所有外设
- 硬件清单中的 USB 设备按名称/描述进一步分类，USB 存储、音频、摄像头等不再统一归入“USB 设备”
- 外设扫描节流状态加锁，修复刷新按钮与后台扫描之间的竞态
- 合并 Win32_OperatingSystem 查询，减少启动时 WMI 调用次数
- 硬件清单支持持久缓存：启动时先显示缓存，TTL 到期后刷新并更新缓存
- 硬件清单缓存改为临时文件原子替换，降低写入中断导致缓存损坏的概率
- 设置与硬件清单缓存启动时会尝试恢复未完成的 `.tmp` 文件，进一步提升崩溃后恢复能力
- 主文件已存在时自动清理残留 `.tmp`，避免临时文件长期堆积
- 临时文件清理失败时仍会读取已存在的主设置/缓存文件，不会因 `.tmp` 权限问题回退默认值
- 硬件清单缓存刷新失败时保留最后一次可用缓存，不再因一次 WMI 异常把整页降级为空
- 硬件清单缓存仅在检测结果变化时重写文件，减少无变化时的磁盘写入
- 加载缓存时把 null 集合归一化为空列表，避免损坏或旧版缓存触发空引用
- 加载缓存时过滤集合中的 null 元素，进一步防止手工损坏的缓存触发空引用
- 设置文件改为临时文件原子替换，降低窗口关闭或保存时异常中断导致设置损坏的概率
- WMI 清单与外设查询增加 20 秒超时，避免个别 WMI 查询卡死导致采集长时间无响应
- 电池名称缺失或为空时直接视为无电池，不再生成空电池项
- WMI 清单与外设查询及时释放 `ManagementObjectCollection`，降低长跑过程中的 WMI 句柄泄漏
- 集成 HidSharp 开源库，补充 HID 外设枚举
- HidSharp 枚举的 HID 设备按名称进一步分类，键盘/鼠标/手柄/摄像头不再一律归入“输入设备”
- 集成 Vanara 开源库，替换手写 Kernel32/DWM P/Invoke（运行时长、内存、电池、Secure Boot、标题栏主题）
- 集成 ManagedNativeWifi 开源库，优先通过原生 WLAN API 读取 Wi-Fi 详情，失败时回退到 netsh
- netsh Wi-Fi 回退路径增加 15 秒退出超时，异常进程会强制结束，避免采集线程长时间阻塞
- netsh Wi-Fi 回退改为先等待退出超时再读取输出，避免读取管道时绕过 15 秒超时
- netsh Wi-Fi 解析兼容全角冒号 `：`，中文 Windows 输出不再因分隔符差异解析失败
- ManagedNativeWifi 当前连接信息增加非空判断，避免极端情况下空引用导致回退失败
- 外设列表改为每个设备独立小框并自动换行，多同类型设备不再混在一列
- 外设去重键改为包含 PNPDeviceID，避免多个同型号物理设备被错误合并
- 外设扫描结果在返回前按 PNPDeviceID 二次去重，避免 HID/键盘/鼠标等多个枚举路径重复同一设备
- 增加设置窗口，可配置实时检测频率、外设扫描间隔与网络噪音过滤
- 设置窗口补充硬件清单刷新间隔、语言与深色主题选项
- 修复设置窗口本地化绑定，避免文案空白
- 设置窗口切换语言时即时预览窗口文案并在取消时恢复原语言，不再保持旧语言标题直到重开
- 修复硬件清单刷新间隔在设置中修改后不生效的问题，改为运行时更新缓存 TTL
- 设置页持久化的 LastPage 增加枚举合法性校验，损坏或非法值回退到概览页
- 设置页持久化的语言字段增加合法性校验，非法语言回退为系统语言
- 恢复窗口位置时同时校验 Left/Top，避免单边字段损坏时写入 NaN 窗口位置
- 恢复窗口尺寸时限制到虚拟屏幕范围，避免损坏设置把窗口放大到屏幕外
- 设置窗口对非预设配置值回退到默认选项，避免下拉框空置
- 合并“其他设备”与“外设”入口，统一由外设页展示
- CPU、内存、硬盘、显卡等硬件清单按单个物理设备分栏展示
- 多显卡场景下总显存、已用显存、空闲显存与占用率改为汇总所有 GPU 传感器，而不是只取第一块
- 显示器外设详情补充分辨率与刷新率
- GPU 分辨率仅在宽高都有有效值时显示，避免出现 `1920 × 0` 这类误导值
- 多显示器场景下仅在单一视频控制器时使用其刷新率，避免给所有显示器误套同一刷新率
- 网络接口直接显示 IP/网关/DNS，未连接接口保留显示并标记“未连接”
- 收紧 CPU 硬件匹配，避免网络设备、AMD Radeon、Intel Arc 等 GPU 被当成 CPU 传感器，同时保留 AMD APU 的 CPU 识别
- CPU/GPU 硬件名称匹配对空名称返回 false，避免传感器数据异常时触发空引用
- NaN/Infinity 指标在 CSV、兼容性报告、GPU 引擎和运行时长中统一显示 N/A，不再输出 `NaN%` 或抛异常
- 兼容性自检报告中“检测到但型号未知”的硬件不再显示矛盾文案 `[OK] ... not detected`
- 运行指标数据源在 provider 层统一过滤 NaN/Infinity，GPU 引擎无有效采样时返回不可用而不是 `0%`
- 网络吞吐与 GPU 引擎聚合仅累加有限值；全部计数器返回 NaN 时显示不可用，而不是 `0 B/s`
- 电池状态改为按位判断 unknown/charging，避免标志位组合时误判充电状态
- 性能计数器释放改为逐个安全释放，避免关闭窗口时某个计数器异常阻塞后续清理
- 运行时长格式化修正：0 秒时显示 `0s`，不再向上取整为 `1s`
- 运行时长格式化对超出 `TimeSpan` 范围的极大值显示不可用，不再触发溢出异常
- 外设页“无外设”提示改为顶部停靠，修复贴到左侧的布局问题
- 外设页首次扫描前总数显示为“不可用”，不再短暂空白
- 外设页切换中英文时立即重建已有列表，修复需要等下一次扫描才更新文案的问题
- 导出写入异常改为状态提示，不再触发未处理异常；导出文件类型文案支持中英切换
- 顶部状态提示复用同一个定时器，避免连续导出时反复创建计时器对象
- 窗口关闭时停止顶部状态提示定时器，避免关闭后仍可能恢复标题
- HTML 报告“键盘 / 鼠标”补充设备名称，不再只显示数量
- HTML 报告内存模块补充“外形”和“ECC”列，与详情页一致
- HTML 报告内存容量字符串为 `0` 时按 N/A 显示，不再输出 `0.0 GB`
- HTML 报告在无内存模块时总容量显示 N/A，不再显示 `0 条 / 0.0 GB`
- TXT 报告在无内存模块时总容量显示不可用，不再显示 `0 B`
- TXT 报告“其他设备”名称只枚举一次，避免重复遍历造成边界错误或开销
- HTML/TXT 报告磁盘信息补充序列号，与详情页一致
- HTML 报告显卡区块补充显存列
- HTML 报告电池信息补充设计/当前电压
- HTML 报告 Wi-Fi 区块补充 BSSID 列
- HTML 报告新增 SMART 属性区块
- HTML 报告网络适配器补充驱动日期
- HTML 报告显示器补充序列号和生产年份
- 发布/构建脚本现在会检查外部命令退出码和产物是否存在，避免失败时仍报告成功
- 第三方声明补充 LibreHardwareMonitor 的 BlackSharp.Core / DiskInfoToolkit / RAMSPDToolkit 传递依赖

### 测试
- 测试覆盖增至 213 项

## [1.1.0] - 2026-08-11

### 新增
- 概览页 12 张卡片：各硬件型号 + 实时使用率/频率/温度/风扇/运行时长/最近更新时刻
- 硬件详情扩充：CPU 架构/缓存/插槽/ID、磁盘介质/总线/固件/健康度、内存 SPD、GPU 显存（传感器）、网卡速率/IP/网关/DNS、电池容量/循环/化学/电压、主板系统信息、显示器 EDID、音频/USB/键盘/鼠标/打印机
- 外设页：热插拔设备自动分类识别（5 秒刷新 + 手动刷新），排除内置硬盘
- 进程页：Top 进程 CPU/内存（含 PID）+ 进程总数
- 导出：JSON / TXT / HTML / 复制摘要 / 兼容性自检报告 / **历史 CSV**
- 中英双语 + 深色/浅色主题（记忆）；窗口位置/大小/主题/语言持久化；软件版本显示
- 软件图标（窗口/exe/安装程序）

### 修复与健壮性
- 滚动卡顿：采集移后台线程、WMI 缓存、页面原地更新、曲线下采样
- 历史环形缓冲瘦身（3600 样本仅 ~32KB）
- 后台 Dispatcher 投递竞态兜底、LHM 懒加载零阻塞启动
- JSON 导出对 NaN/Infinity 传感器值不再抛异常；界面格式化防 NaN
- 关键温度/显存/电池容量等数据源修正（单位校验、传感器优先、root\wmi 字段名修正）
- 单实例、全局异常日志（256KB 截断）、数据源异常横幅（性能计数器/WMI）
- 导出/界面全层不变区域格式化（统一到 FormatUtil，跨语言环境稳定）
- 运行时长导出格式（不足 1 天显示 HH:mm）、概览温度卡不变区域
- 已提权时隐藏「以管理员权限重启」按钮；单实例/异常弹窗本地化
- 提权重启竞态修复：新实例等待旧实例释放单实例互斥锁，UAC 取消时保持当前会话
- 左侧导航栏随主题联动变色（深色→深底浅字，浅色→浅底深字），选中/悬停同步主题
- 修复顶栏「导出」菜单文字在深色头部下看不清的问题（顶部项浅色文字、子菜单弹层深色文字）
- 修复网卡速率哨兵值（long.MaxValue）被显示为 9223372036.9 Gbps 的问题；概览页主网卡改为优先真实有线/无线网卡（排除 TAP/隧道/虚拟设备）
- 修复德语/法语等区域下部分数值显示逗号小数点（CPU 基频/电池容量电压/风扇/进程占用/内存电压等全部改为不变区域格式化，与界面其它数值一致）
- 外设页扫描进行中再点「手动刷新」不再被丢弃（标记待办，当前扫描完成后立即再扫）；README 中英补充快捷键说明（Ctrl+E / F5 / F11）
- 传感器页新增「隐藏网络过滤器噪音」开关（默认关闭，不改变默认显示；开启后过滤 NDIS/WFP/QoS/Leigod/Npcap/VirtualBox 等过滤器栈重复计数）；新增边缘用例测试 6 项
- 「隐藏网络过滤器噪音」开关持久化（与主题/语言一致，重启后保留）
- 清理 PeripheralProbe 可空性警告，构建达到 0 警告 0 错误
- 深色/浅色主题化滚动条（圆角滑块随主题变色、轨道透明、无可滚动内容自动隐藏），替换系统浅色滚动条
- 主题化按钮（圆角、悬停/按下/聚焦/禁用状态随主题变色，替代系统浅灰按钮）
- 主题化复选框（勾选框随主题变色）与 GroupBox 边框（改用 CardBorderBrush，替代默认浅色边框）
- 无障碍：导航项自动化名称改为纯标题（屏幕阅读器不再读出 NavEntry 记录串）
- 窗口标题栏随深浅主题变色（DWM 沉浸式深色，Windows 10 2004+/11 及旧版 1809-1903 兼容）
- 性能优化：CPU 占用从 ~23% 降至 ~10% 单核 —— 进程枚举 5 秒节流、GPU Engine（数百计数器实例）2 秒节流、传感器 2 秒/SMART 5 秒缓存；实时指标仍 1 秒刷新
- 性能计数器改为首次采集时延迟创建，避免主窗口构造阶段阻塞 UI，进一步缩短启动到可交互的时间
- 运行时长不足 1 分钟时按秒显示（修复 30 秒显示成「1 分」），新增 7 项 uptime 单测
- 新增 13 项 HardwareClassifier 单测（主 GPU 挑选/虚拟显卡排除/CPU·GPU 硬件匹配）
- 新增 3 项 LocalizationManager 单测（缺失 key 回退/语言切换事件/非法语言回退 en）
- README 中英补充传感器页「隐藏网络过滤器噪音」开关说明
- 新增 21 项外设分类单测（PNPClass/HID/USB 描述分类、未知回退 other）
- 外设扫描节流 5s→10s（Win32_PnPEntity 枚举 ~650ms，减半开销），README 同步更新
- 新增 10 项格式边界/CSV 空值单测（Bytes 单位边界、历史 CSV null 值/空历史）
- 记住上次浏览的页面（重启后回到上次所在页，与应用记住主题/语言一致）
- 新增 3 项 CachedInventoryProvider 单测（TTL 内缓存/过期重采/内部返回 null 兜底）
- TXT/HTML 导出：CPU 缓存/当前频率未知时显示 N/A（不再显示误导性的 0KB/0MHz），新增 3 项单测
- 新增双语 RELEASE_NOTES.md（GitHub Release 发布说明）
- 提升左侧导航栏与主背景的对比度，切换深浅主题时左侧栏变化更明显；补上 F11 快捷键切换主题（与主题按钮一致）
- 概览内存卡无内存模块时不再显示 (0×)
- 概览页首次采集前更新时间显示为“不可用”，不再闪现 `00:00:00`
- 进程页首次采集前总数显示为“不可用”，不再误显示 `0`
- 概览页“实时刷新中”文案跟随设置中的实际刷新间隔，不再固定显示 1 秒
- 头部导出按钮收敛为「导出」菜单（避免窄窗口溢出）

### 测试
- 测试覆盖 23 项（采集聚合/降级/SMART/历史瘦身/导出/JSON 非有限值/null 返回/空快照/历史 CSV）

### 工程
- 统一不变区域格式化到 Core.FormatUtil（消除重复）
- 新增 scripts/test.ps1、scripts/README.md、CHANGELOG.md、.gitattributes

## [1.0.0] - 2026-08-10

### 新增
- 初始版本：硬件清单 + 实时运行指标监控
- WPF 中英双语界面（概览 + 分类详情 + 历史曲线）
- 数据源：性能计数器 + 原生 API、WMI、LibreHardwareMonitor
- 导出 JSON / TXT；Inno Setup 安装程序
